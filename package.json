{
  "name": "tdp-round-mapper",
  "version": "1.0.0",
  "private": true,
  "description": "TDP \u2014 Train Don't Play \u00b7 Round Mapper (iOS via Capacitor)",
  "type": "module",
  "scripts": {
    "build:www": "node scripts/build-www.mjs",
    "sync": "npm run build:www && cap sync ios",
    "ios": "npm run sync && cap open ios",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@capacitor-community/apple-sign-in": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/preferences": "^6.0.4",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  }
}
